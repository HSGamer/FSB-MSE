1. cài thư viện từ file requirements.txt bằng lệnh: pip install -r requirements.txt
2. train và báo cáo model bằng lệnh: python model_build.py
3. chạy model bằng lệnh: python model_gui.py
4. chọn các ảnh mẫu trong thư mục sample để test model
